'use strict';
const fs=require('fs'),path=require('path');
const root=path.resolve(__dirname,'..');
const read=rel=>fs.readFileSync(path.join(root,rel),'utf8');
const write=(rel,text)=>{const p=path.join(root,rel);fs.mkdirSync(path.dirname(p),{recursive:true});fs.writeFileSync(p,text);};
function replaceOnce(source,before,after,label){
  const i=source.indexOf(before);
  if(i<0) throw new Error('missing expected source: '+label);
  if(source.indexOf(before,i+1)>=0) throw new Error('expected unique source but found duplicate: '+label);
  return source.slice(0,i)+after+source.slice(i+before.length);
}
function replaceSpan(source,startMarker,endMarker,replacement,label){
  const start=source.indexOf(startMarker),end=source.indexOf(endMarker,start);
  if(start<0||end<=start) throw new Error('missing span: '+label);
  return source.slice(0,start)+replacement+source.slice(end);
}

let renderer=read('src/domain/notation/notation-renderer.js');
const rendererInsert=`function drawKeySignature(svg,key,signatureMode,staff,options){\n  if(signatureMode!=="shown"||key.acc===0)return;\n\n  const symbols=key.type==="sharp"?options.sharpOrder.slice(0,key.acc):options.flatOrder.slice(0,key.acc);\n  const sharpSteps={F:8,C:5,G:9,D:6,A:3,E:7,B:4};\n  const flatSteps={B:4,E:7,A:3,D:6,G:2,C:5,F:1};\n  const glyph=key.type==="sharp"?options.sharpGlyph:options.flatGlyph;\n\n  symbols.forEach((letter,i)=>{\n    const step=key.type==="sharp"?sharpSteps[letter]:flatSteps[letter];\n    const y=staff.top+staff.spacing*4-step*(staff.spacing/2);\n\n    svg.appendChild(createSvgElement("text",{\n      x:staff.sigX+i*options.advance,\n      y,\n      "font-size":options.fontSize,\n      "font-family":options.fontFamily,\n      fill:"#111",\n      class:"key-signature-glyph",\n      "pointer-events":"none"\n    },glyph));\n  });\n}\n\nfunction rightEdgeOfSvgClass(svg,className){\n  let right=-Infinity;\n  svg.querySelectorAll("."+className).forEach(node=>{\n    try{\n      const box=node.getBBox();\n      right=Math.max(right,box.x+box.width);\n    }catch(err){}\n  });\n  return Number.isFinite(right)?right:null;\n}\n\nfunction drawTimeSignature(svg,staff,x,options){\n  const box=options.measureText(svg,options.glyph,options.fontSize,options.fontFamily);\n\n  // Visible left edge starts exactly at x.\n  const textX=x-box.x;\n  const centers=[\n    staff.top+staff.spacing,\n    staff.top+staff.spacing*3\n  ];\n\n  centers.forEach(centerY=>{\n    const textY=centerY-(box.y+box.height/2);\n    svg.appendChild(createSvgElement("text",{\n      x:textX,\n      y:textY,\n      "font-size":options.fontSize,\n      "font-family":options.fontFamily,\n      fill:"#111",\n      class:"time-signature-glyph",\n      "pointer-events":"none"\n    },options.glyph));\n  });\n}\n\n`;
renderer=replaceOnce(renderer,'app.notationRenderer=Object.freeze({\n  NS,createSvgElement,accidentalSmuflGlyph,drawLedger\n});',rendererInsert+'app.notationRenderer=Object.freeze({\n  NS,createSvgElement,accidentalSmuflGlyph,drawLedger,\n  drawKeySignature,rightEdgeOfSvgClass,drawTimeSignature\n});','renderer API extension');
write('src/domain/notation/notation-renderer.js',renderer);

let trainer=read('src/trainer.js');
const originalSignature=trainer.slice(trainer.indexOf('function drawSignature('),trainer.indexOf('function rightEdgeOfSvgClass('));
const originalRight=trainer.slice(trainer.indexOf('function rightEdgeOfSvgClass('),trainer.indexOf('function drawTimeSignature('));
const originalTime=trainer.slice(trainer.indexOf('function drawTimeSignature('),trainer.indexOf('function drawLedger('));
if(!originalSignature||!originalRight||!originalTime) throw new Error('renderer source spans not found');
const signatureWrapper=`function drawSignature(svg,key,signatureMode,staff){\n return notationRenderer.drawKeySignature(svg,key,signatureMode,staff,{\n   sharpOrder:SHARP_ORDER,\n   flatOrder:FLAT_ORDER,\n   sharpGlyph:SMUFL.accidentalSharp,\n   flatGlyph:SMUFL.accidentalFlat,\n   fontSize:musicEm(staff),\n   fontFamily:SMUFL_FONT,\n   advance:sp(staff,1.05)\n });\n}\n\n`;
const rightWrapper=`function rightEdgeOfSvgClass(svg,className){return notationRenderer.rightEdgeOfSvgClass(svg,className);}\n\n`;
const timeWrapper=`function drawTimeSignature(svg,staff,x){\n return notationRenderer.drawTimeSignature(svg,staff,x,{\n   glyph:SMUFL.timeSig4,\n   fontSize:musicEm(staff),\n   fontFamily:SMUFL_FONT,\n   measureText:measureSvgTextGlyph\n });\n}\n`;
trainer=replaceSpan(trainer,'function drawSignature(','function rightEdgeOfSvgClass(',signatureWrapper,'drawSignature');
trainer=replaceSpan(trainer,'function rightEdgeOfSvgClass(','function drawTimeSignature(',rightWrapper,'rightEdgeOfSvgClass');
trainer=replaceSpan(trainer,'function drawTimeSignature(','function drawLedger(',timeWrapper,'drawTimeSignature');
write('src/trainer.js',trainer);

const restore=`'use strict';\nconst assert=require('assert');\nconst originals=${JSON.stringify({signature:originalSignature,right:originalRight,time:originalTime})};\nfunction replaceExact(source,after,before,label){\n  const i=source.indexOf(after);assert(i>=0,'missing v0.8.1-b2 edit: '+label);\n  assert.equal(source.indexOf(after,i+1),-1,'duplicate v0.8.1-b2 edit: '+label);\n  return source.slice(0,i)+before+source.slice(i+after.length);\n}\nmodule.exports=function restoreStaticSignatures(source,file){\n  if(file!=='src/trainer.js') return source;\n  source=replaceExact(source,${JSON.stringify(signatureWrapper)},originals.signature,'drawSignature');\n  source=replaceExact(source,${JSON.stringify(rightWrapper)},originals.right,'rightEdgeOfSvgClass');\n  source=replaceExact(source,${JSON.stringify(timeWrapper)},originals.time,'drawTimeSignature');\n  return source;\n};\n`;
write('tests/helpers/restore-renderer-static-signatures.cjs',restore);

let boundary=read('tests/notation-boundary.test.js');
boundary=replaceOnce(boundary,"const restoreRenderer=require('./helpers/restore-renderer-foundation.cjs');\nfor(const file of Object.keys(boundary.files))restore(restoreRenderer(read(file),file),file);","const restoreRenderer=require('./helpers/restore-renderer-foundation.cjs');\nconst restoreStatic=require('./helpers/restore-renderer-static-signatures.cjs');\nfor(const file of Object.keys(boundary.files))restore(restoreRenderer(restoreStatic(read(file),file),file),file);",'notation boundary restore chain');
write('tests/notation-boundary.test.js',boundary);

let domainTest=read('tests/major-scale-domain.test.js');
domainTest=replaceOnce(domainTest,"const restoreRenderer=require('./helpers/restore-renderer-foundation.cjs');\nlet restored=require('./helpers/restore-notation-baseline.cjs')(restoreRenderer(trainer,'src/trainer.js'),'src/trainer.js');","const restoreRenderer=require('./helpers/restore-renderer-foundation.cjs');\nconst restoreStatic=require('./helpers/restore-renderer-static-signatures.cjs');\nlet restored=require('./helpers/restore-notation-baseline.cjs')(restoreRenderer(restoreStatic(trainer,'src/trainer.js'),'src/trainer.js'),'src/trainer.js');",'major-scale restore chain');
write('tests/major-scale-domain.test.js',domainTest);

const test=`'use strict';\nconst fs=require('fs'),path=require('path'),vm=require('vm'),assert=require('assert');\nconst root=path.resolve(__dirname,'..'),read=rel=>fs.readFileSync(path.join(root,rel),'utf8');\nconst source=read('src/domain/notation/notation-renderer.js');\nassert(!/supabase|fetch\\s*\\(|XMLHttpRequest|WebSocket|\\beval\\s*\\(|new Function|import\\s*\\(|MAJOR_SCALE_NOTATION|LEVEL_KEYS|STAGE_[1-4]/i.test(source),'renderer must stay presentation-only');\nconst document={createElementNS(ns,name){return{ns,name,attrs:{},textContent:'',setAttribute(k,v){this.attrs[k]=v;}};}};\nconst ctx={window:{},document};vm.createContext(ctx);vm.runInContext(source,ctx,{filename:'notation-renderer.js'});\nconst api=ctx.window.MajorScaleApp.notationRenderer;assert(api&&Object.isFrozen(api));\nconst mkSvg=()=>({children:[],appendChild(n){this.children.push(n);}});\nlet svg=mkSvg();\napi.drawKeySignature(svg,{type:'sharp',acc:2},'shown',{top:100,spacing:20,sigX:130},{sharpOrder:['F','C','G','D','A','E','B'],flatOrder:['B','E','A','D','G','C','F'],sharpGlyph:'S',flatGlyph:'F',fontSize:80,fontFamily:'TrainerBravura',advance:21});\nassert.equal(svg.children.length,2);assert.deepStrictEqual(svg.children.map(n=>n.textContent),['S','S']);\nassert.deepStrictEqual(svg.children.map(n=>[n.attrs.x,n.attrs.y]),[[130,100],[151,130]]);\nsvg=mkSvg();api.drawKeySignature(svg,{type:'flat',acc:2},'hidden',{top:100,spacing:20,sigX:130},{sharpOrder:['F'],flatOrder:['B','E'],sharpGlyph:'S',flatGlyph:'F',fontSize:80,fontFamily:'TrainerBravura',advance:21});assert.equal(svg.children.length,0);\nsvg=mkSvg();api.drawKeySignature(svg,{type:'flat',acc:2},'shown',{top:100,spacing:20,sigX:130},{sharpOrder:['F'],flatOrder:['B','E'],sharpGlyph:'S',flatGlyph:'F',fontSize:80,fontFamily:'TrainerBravura',advance:21});assert.deepStrictEqual(svg.children.map(n=>[n.textContent,n.attrs.x,n.attrs.y]),[['F',130,140],['F',151,110]]);\nconst nodes=[{getBBox:()=>({x:10,width:20})},{getBBox:()=>({x:35,width:12})},{getBBox(){throw new Error('hidden')}}];\nassert.equal(api.rightEdgeOfSvgClass({querySelectorAll(sel){assert.equal(sel,'.time-signature-glyph');return nodes;}},'time-signature-glyph'),47);\nassert.equal(api.rightEdgeOfSvgClass({querySelectorAll(){return[];}},'x'),null);\nsvg=mkSvg();api.drawTimeSignature(svg,{top:100,spacing:20},150,{glyph:'4',fontSize:80,fontFamily:'TrainerBravura',measureText(){return{x:-3,y:-20,width:15,height:40};}});\nassert.equal(svg.children.length,2);assert.deepStrictEqual(svg.children.map(n=>[n.textContent,n.attrs.x,n.attrs.y]),[['4',153,120],['4',153,160]]);\nconst trainer=read('src/trainer.js');\nassert(trainer.includes('return notationRenderer.drawKeySignature'));assert(trainer.includes('return notationRenderer.rightEdgeOfSvgClass'));assert(trainer.includes('return notationRenderer.drawTimeSignature'));\nfor(const forbidden of ['function drawBeams(){','function render(){','scoreSvg.addEventListener("pointerdown"'])assert(trainer.includes(forbidden),'critical legacy runtime must remain: '+forbidden);\nconsole.log('PASS notation renderer static signatures: key signature, time signature, measured right edge, wrappers and critical legacy boundaries');\n`;
write('tests/notation-renderer-static-signatures.test.js',test);
console.log('Applied v0.8.1-b2 static signature extraction');
